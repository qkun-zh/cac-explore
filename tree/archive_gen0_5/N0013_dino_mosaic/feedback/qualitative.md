# qualitative feedback — N0013_dino_mosaic (parent N0010_dino_multilayer_long 21.531@E26)

## reasoning
- Outcome: best 22.404 @E38 (final 22.761, RMSE/MAE 3.62), clean 40ep run (1410s, 23.11M, no OOM/instability) → +0.87 (+4.1%) REGRESSION vs parent. Primary falsification bar of idea.md's H0022-augreg claim ("DISPROVED IF MAE >21.53") is breached.
- Mechanism read: regularization demonstrably engaged — train-loss floor 8.21 vs parent ~7.8 on identical data/objective, best epoch shifted 26→38 (the ">26" sub-claim holds), yet val plateaued ~22.4–24 from E18 with ±1.5 noise and train→val divergence persisted (loss −43% while val flat): overfit-gap promise NOT delivered. Tail mechanism dead: RMSE/MAE 3.62 ≫ required <3.4 (parent 3.63); MAE≤20 missed by +12%.
- Trajectory trap: STATE's "tracking ahead of champion" (E13 24.38 vs parent 25.50) reversed by E14–17 (27–28) and never recovered. Slower convergence is the EXPECTED effect of jitter+wd5e-4 — early-epoch leads are uninformative for regularized variants. (Local log tail starts E13; server log authoritative for E1–12.)
- Code quality: naming/docstrings good (module docstring model.py:1; both jitter helpers documented :64,:114; cfg keys mirror idea.md 1:1). Training-gating correct: self.training checks, train() override keeps frozen backbone eval (:58–61), jitter applied before no_grad backbone pass (intended — perturbs frozen features too). Defects: (a) per-sample Python loops with .item() (:81–111,:121–146) force GPU syncs every step (+~11% wall-clock vs parent 1275s); (b) inconsistent gating — batch-level coin flip for photometric (:69) vs per-sample for bbox (:122) — undocumented, halves aug diversity when the batch flip loses; (c) try/except dual code path (torchvision vs manual :74–107) silently diverges across envs and hides real dtype bugs; comment "keep SMOKE deterministic-ish" (:67) is false — torch.rand is still random; (d) bbox scale-up can invert center clamp when w_new>S (:134–138) → degenerate full-frame box (harmless, sloppy). No correctness bug found — results trustworthy.
- Protocol/spec: MODIFICATIONS.md absent — PROTOCOL §2 contract does not list it → compliant. Spec compliance exact (taps 6/11, scalar gate, adapter768 d0.2, wd5e-4, count_w1.0, 392px, jitter params = idea defaults, +0 params).
- HYPOTHESIS-ID COLLISION confirmed: idea.md:20 labels the augreg hypothesis "H0022", but memory/hypotheses.jsonl:37 already books H0022 = tail-aware reweighting (tags tail-reweight/N0011). Max banked id = H0023 → must be booked as NEW H0024; evidence must not touch H0022's confidence.

## actionable_feedback
- Synthesis/Lead: book augreg verdict as `create H0024` (contradicts, below); add failure_modes entry "idea agents derive hypothesis ids from memory/index.json max-id, never reuse booked ids".
- Retry design (if augreg track survives gen-5): halve dose (noise σ0.01, bbox_jitter 0.08–0.10, prob 0.3) or anneal off after ~E20; ideally ablate wd-only first — dropout+wd+jitter landed together, so the negative effect is confounded across three levers.
- Perf: vectorize both jitters (torch.rand(B,1,1,1) factors, batched tensor ops); delete the try/except fallback; unify gating to per-sample p; fix the smoke-determinism comment. Reclaims ~2min/run.
- Robustness: cap bbox scale so w_new,h_new ≤0.95·S before center clamping.
- Process: judge regularized-node trajectories only after E≥25 (or by best-so-far slope); update STATE's early-trajectory heuristic which mispredicted this endgame. N0014/N0015/N0016 should NOT inherit augreg at current strengths.

## hypothesis_updates
- hypothesis_id: H0022-as-written-in-idea.md (book as NEW H0024 due to collision) — evidence_type: contradicts — strength: 0.80 — reasoning: full-schedule clean run breaches explicit disproof bar (22.404>21.53); THEN-clauses MAE≤20 (+12% miss) and RMSE/MAE<3.4 (observed 3.62) both fail; only best-epoch>26 supported (38) — mechanism engaged, net-negative effect.
- hypothesis_id: H0017 — evidence_type: neutral — strength: 0.30 — reasoning: dual-tap scalar-gate stack ran as designed under augmentation (no instability), but gate weights are not logged anywhere in artifacts, so the "gate balance persists" claim is unobservable; no confidence update beyond n_tested++.
- hypothesis_id: NEW-proposal (suggest H0025) — evidence_type: neutral — strength: 0.00 — reasoning: scoped follow-up worth one slot IF gen-5 keeps an augreg track: "IF low-dose annealed jitter (prob≤0.3, σ0.01, off after E20) IN N0010 champion stack, THEN MAE ≤21.5 AND best epoch>30, BECAUSE late-phase-only variance regularizes without degrading feature fidelity. DISPROVED IF MAE>22."
