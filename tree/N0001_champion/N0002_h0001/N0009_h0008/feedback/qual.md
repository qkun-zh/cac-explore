# N0009_h0008 — qualitative feedback (text-log training-dynamics read)

- **Node:** N0009_h0008 (H0008 `use_hires` post-decoder detail residual, solo on `use_simprior=true`) · **Parent:** N0002_h0001 (22.5641 @ep30).
- **Outcome:** 26.4535 @ep31, 32/32ep, `budget_hit=true`, `code=ok`, elapsed 1809.7s / 1815.2s (+3.89 vs parent, bar 22.26 missed by 4.19).
- **Sources read:** `idea.md` §§0–3 inspection list; `result.json` + `info.json` (timeout, metrics above); server text log `/tmp/train-b4-n0009.log` (567 lines, single-grep reads); server TB scalars full 32-epoch series for child + parent (`val/mae`, `val/rmse`, `train/loss_epoch`, `train/mae`); `best.pth` CPU weight norms (child ep31 + parent ep30 for paired contrast); `model.py` hires wiring lines. No stored feature dumps exist (AGENTS §9); this read is text-log only.

## 1. Trajectory shape: immediate separation, then a flat underfit plateau — not overfit

- Full `val/mae` series (child vs parent, same seed/protocol):
  child `[141.48, 76.15, 54.97, 44.41, 37.68, 33.62, 31.04, 29.36, 28.32, 27.65, 27.24, 26.98, …]` → flat `[26.46×8]` to 26.4535 @ep31;
  parent `[153.29, 70.05, 48.79, 39.12, 33.50, 30.30, 28.41, 27.27, 26.29, 25.41, 24.68, 24.26, …]` → 22.5641 @ep30.
- Two shape facts matter. First, ep0 inverts (child 141.5 < parent 153.3) then ep1 flips hard (76.15 vs 70.05, +6.10) and the child never recovers: +6.18 @ep2, +5.29 @ep3, +4.18 @ep4, +3.33 @ep5, then a steady +2–2.7 band ep6–11 widening to +3.89 at the end. Unlike the sibling simcal node (5 tracking epochs then separation), there is no tracking window here — separation is immediate at ep1. The zero-init contract (step-0 forward ≡ parent *architecture at init*, not trained parent weights) predicts only init-level ep0 noise, which is what the ep0 inversion is; the ep1+ gap is learning dynamics, not init.
- Second, the child descends monotonically but stalls early: gains per epoch shrink to ~0.2 by ep10 and to ~0.00 over the last ~10 epochs (26.46 flatline). No U-turn, no spike, no late recovery — a floor, not a minimum that was then lost.
- Decisive against overfit: `train/loss_epoch` stalls with it. Child: `48.94 → 8.40 → 7.65 → 8.29 (uptick ep3) → 6.65 → 5.96`, then 5.92–6.00 flat for the remaining ~26 epochs (last value 6.00). Parent: `38.02 → 8.65 → … → 5.49 @ep9 → … → 2.62 @ep31`, descending every late epoch. Train-worse AND val-worse by a wide margin (terminal 5.96 vs 2.62) is optimization drag / underfit, not memorization. The residual made fitting harder, not easier.
- Tail concurs: child `val/rmse` `166.04 → 120.92 → 110.10 → 104.61 → 101.08 → 98.86 → 97.71 → 97.13 → 96.84 → 96.73 → 96.69 → 96.67` then flat-rises to 96.87 at the end — it never descends past ~96.6. Parent descends throughout to ~88.9 before a mild late drift to 89.24. The dense tail never improves under hires; the intended "separable humps" never materialize in the metric.

## 2. Clean vs dirty: CLEAN run, plateau-trustworthy number

- Clean by the TB record: zero NaN/inf in any series over all 32 epochs (contrast simcal's ep10 synchronized NaN wall), no loss spike, no restart. The ep3 train uptick (7.65→8.29) is the only wrinkle and it re-descends the next epoch — ordinary, not a blow-up.
- `timeout` is bookkeeping, not truncation harm: 32/32 epochs stepped, best @ep31-of-32 on a ~10-epoch flatline (last 8 val/mae readings all 26.45–26.46), log tail shows both `[budget] exceeded 1800s` and `` `Trainer.fit` stopped: `max_epochs=32` reached `` — the wall clock fired during post-epoch bookkeeping, same pattern as the verify sibling. Nothing about the shape suggests more epochs would close a 3.89 gap.
- Best-pick is an edge-adjacent plateau sample (ep31, neighbors identical at noise level), so it carries no selection meaning beyond "the floor is 26.45" — but the floor itself is trustworthy. The 26.4535 stands as a usable paired-contrast number: a clean miss of the 22.26 bar by miles.

## 3. Engaged vs ignored: engaged — and it dragged the readout with it (distinct hires angle)

- TB logs no `hires/*` diagnostic scalars, so the curve alone cannot answer null-vs-engaged — but the checkpoint can. Against the spec'd zero-inits (`Head` weight+bias exactly 0 at step 0), the ep31 weights have moved far: `head.hires.head.weight` norm 0.2250 (meanabs 0.0406, maxabs 0.068), `head.bias` 0.0461; guide/ref conv norms 3.70 / 3.40 / 3.10, GN weights ~4.5–4.6. The residual learned a non-trivial function; the optimizer did not discard it. Combined with §1, the reading is active-but-harmful: a live detail path that stalls both train and val. That closes idea §2's "head norm ≈ 0 with train ≈ parent ⇒ resolution not binding" null branch — this is the engaged-but-worse branch.
- Isolation check (idea §2 required simprior q/k norms + temp to match the parent, else harness failure): q/k norms hold parity (child 6.71/6.80 vs parent 5.73/7.36 — same order, no freeze-at-half signature of the h2pool catastrophe), `out` norm grows (0.933 vs 0.587), but **temperature collapses 0.0393 → 0.0033**, ~10× sharper and pressed against the 1e-3 clamp floor. Parent and verify-sibling temps sit at 0.039 / ~0.021; 0.0033 is a new extreme — the K-exemplar softmax went near-one-hot. Detached inputs (logits.detach + h2.detach, own parameters only, no shared projection) did not isolate *dynamics*: joint optimization still drove the confirmed readout's selection peaky while the detail path engaged. Whether the peaky readout caused the stall or the stall drove the peak is the causal read's job; as pure shape+checkpoint it says the ban held structurally (wiring verified below) but not dynamically.
- Wiring verified in `model.py`: `logits = decoder.forward_logits(feat)` split preserved, `r = hires(logits.detach(), h2.detach())`, `dens = softplus(logits + r)`, flag-off path `softplus(logits)` op-identical to parent; `HiResDetail` constructed after all parent modules; GCA path and decoder in_ch (192) untouched. No structural anomaly; the failure is dynamical, not a wiring bug.

## 4. Anomalies and process gaps

- `train/mae` is NaN for all 32 epochs on BOTH child and parent — the standing logging bug already noted on three siblings, still unfixed, still depriving qual reads of a train-side counting signal. Ignore the series; do not cite it.
- The text log is dynamics-silent: single-grep anomaly scan returns only the standing benign set — 151 modules in eval mode (frozen backbone, expected), `MeanMetric.compute before update`, CUDA TensorCore tip, `num_workers` bottleneck tip, NumPy non-writable-tensor warning (suppressed after first trigger). Zero `traceback`, zero `error`, zero OOM/CUDA failure. Progress-bar lines carry no per-epoch scalars; only the final line prints `val/mae: 26.454`. Anyone re-reading from logs alone sees a clean `max_epochs=32 reached` + `code=ok` with no hint of the ep1 separation or the train-loss stall — TB parsing was load-bearing for every number in §1.
- Smoke-vs-train consistent: `[smoke] one step fits: ok (model=Counter)`, 31.4M total / 3.5M trainable, seed 20260830 at the top. Param card adds ~15k over the parent's 31.35M-class total — inside budget, not a capacity story.
- Minor: server `run/latest/` holds `best.pth` + TB but no `result.json` (record lives at the node root / local copy) — same sync artifact as the sibling, noted so the next reader does not go hunting.

## One-line assessment

- Clean run, engaged-but-harmful residual: immediate ep1 separation (+6.1) settling onto a joint train (~5.96) / val (26.45) underfit floor with the tail never descending, hires head verifiably non-zero yet the supposedly isolated simprior temperature collapses to 0.003 near the clamp floor — 26.45 @ep31 is a plateau-trustworthy clean miss of 22.26, with no truncation, no NaN, and no wiring excuse.
