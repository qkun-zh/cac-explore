# Feedback — N0061_countnorm (Qual)

Verdict: FAIL. Root cause is **premise error (c)** — the promised mechanism never ran; (a)(b) are proximate degraders on top of a structurally wrong bet.

1. **(c) dominates.** "MSE becomes count-weighted, shape error count-free" requires a unit-mass shape vs a unit-normalized target, but the loss is `MSE(dens, gt_d)` where gt_d = raw Gaussian blobs summing to c (train.py:344–345), NOT c-normalized. A multiplicative `dens→f·dens` rescales the output; it cannot reweight the target's c² curvature. Weighting lives on the target axis (the unused `tail_reweight`, train.py:334); an output multiplier can't replicate it. The tail-steering mechanism never existed.
2. **(a) axis collision with GCA.** Output = `f·(d + bias_n_aux)`, both count knobs reading the SAME interface GAP(fine)+e_mean; L1(f·(s+0.02n_aux),c) is one equation, two unknowns → non-unique fixed points, split attribution, destructive interference — countnorm scaled while GCA counter-adjusted, oscillating per-pixel MSE.
3. **(b) range hurts exactly the tail it claimed to help.** f∈[e⁻²,e²]=[0.14,7.4]; a wrong f≈0.14 on a dense image collapses all pixel mass — worst MSE precisely on high-c images (error²∝c²), and the ±2 clamp zeroes z-gradients at the boundary so the arm stalls while GCA pushes.
4. **Re-run, one new arrangement.** N0037 N·p (20.99), N0029 loghead, N0041 exemplar-sum (24.06), N0061 residual-autoscale — all separate count from shape at the readout. Novel only in applying the multiplier ON the champion's own count-corrected output (a second count mechanism on the same axis), explaining +3.86 vs N0037's +0.5 (independent count, no collision).
5. **Closure.** Readout count-normalization is CLOSED here: four instances (factorized, log, per-exemplar, residual-scalar) all fail. GCA — the 0.02-attenuated additive global offset (zero-init, never a scale) — remains the unique positive count-side component; every non-additive readout count operator is now refuted. Do not retry multiplicative/scale count readouts.